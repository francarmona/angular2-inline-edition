import { InlineEditableDirective } from './inline-editable.directive';

describe('InlineEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineEditableDirective();
    expect(directive).toBeTruthy();
  });
});
